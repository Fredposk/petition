

<div class="mb-2 text-sm font-bold leading-5 text-center text-gray-800 uppercase">Please sign below:</div>


<form method="post" id="form">
  <canvas class="bg-blue-200 border-2 border-gray-500 border-dotted rounded-md shadow-lg" id="canvasSig" width="400" height="200"></canvas>
  <input type="hidden" id="signature" name="signature" value="">
  <input type="hidden" name="_csrf" value="{{csrfToken}}">
  <div class="text-center">
  <button id="sendBtn" class="px-2 py-1 mt-3 mb-2 text-sm tracking-wide text-gray-300 uppercase transition duration-500 bg-gray-500 border-2 border-gray-500 rounded shadow hover:bg-gray-700 hover:shadow-2xl hover:text-gray-400" type="submit">click to submit</button></div>
</form>
<button id="clearBtn" href="/signers" class="warningBtn"
>Clear signature</button>
<script src="/script.js"></script>


{{#if hasUserSigErrors}}
</div>
<div class="flex flex-col items-center justify-center mt-12">
  <ul class="errorEl">
{{#each errors}}
    <li>{{this.msg}}</li>
{{/each}}
{{/if}}


{{#if hasDBErrors}}
</div>
 <div class="flex flex-col items-center justify-center mt-12">
  <ul class="errorEl">
    <li>{{errors}}</li>
{{/if}}
  </ul>
</div>

